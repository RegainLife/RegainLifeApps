# FastMirror 0.3.9 (2026-05-07)

## 概要
- Google Drive 仮想ファイルシステム上のフォルダを監視元にしていると、長時間アイドル後に自動同期が止まる問題を修正しました。

## 変更内容
- Google Drive（マイドライブ等）の監視に `PollWatcher`（定期スキャン方式、30 秒間隔）を使用するようにしました。
- 通常のローカルドライブは従来通り `ReadDirectoryChangesW` による即時検知を維持します。
- その他、コード警告を整理しました。
