# FastMirror 0.3.5 (2026-03-04)

## 概要
- トレイ格納中に周期アップデートチェックが進まず、更新受信できない問題を修正しました。

## 変更内容
- hidden 常駐ループ内で `maybe_run_periodic_update_check()` / `poll_update_check_result()` / `maybe_apply_scheduled_update()` を実行するよう修正。
- これにより、トレイ格納中でも5分周期の更新チェックと自動適用フローが継続します。
